# 2026-09-21 对话与附件更新

- 恢复纯白背景，保留顶部品牌区域分隔线。
- 文件预览去掉演示说明行，返回/下载默认灰底；长文件名支持展开；附件卡片显示格式和实际文件大小。
- 输入框两条提示轮播，独立 / 呼出方案选择；选中方案不切换提示。输入及提示统一 16px。
- 待发送附件移入输入框，多文件换行；附件类型使用指定图片/PDF/Word/Excel 图标；附件名 12px，保留后缀并支持查看全名。
- 用户消息内展示附件；双方文本消息下显示复制按钮，24px 图标与气泡两侧对齐；成功图标保持 2 秒，保留成功文字提示。
- 同步用户替换的上传、语音、侧栏收起、更多 SVG，并更新相关缓存引用。
- 验证：33 项自动测试通过；390px 视口及 Web 实际点击、粘贴、成功态、字号和边缘对齐验证通过。
