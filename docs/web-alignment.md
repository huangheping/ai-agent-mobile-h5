# Web AI Agent 与独立 H5 对齐记录

来源全程只读；只把需要的资源复制到独立 H5，不运行时依赖来源路径。

| 项目 | 当前处理 |
| --- | --- |
| 用户气泡、发送按钮 | Web 当前 #2F3640；白色文字 |
| 品牌与高亮 | #005D56、#24D4C9；方案和提问边框沿用 #13B2BB → #FBB08E |
| 表格 | Web 灰白表头 #F7F8FC、边框 #E1E3EA、隔行 #FAFAFA |
| Agent 头像、结果头像 | 原始 Agent头像.svg / Agent结果头像.svg |
| 做方案、上传附件、新建、关闭、完成状态 | 原始 SVG 文件，复制到 assets |
| 空白页引导图片、卡片箭头 | 原始 你可以这样问.svg / 跳出Icon.svg |
| 历史、发送图标 | 从 Web umi.0b0663b5.js 中提取对应 outlined 图标原始路径 |
| 复制图标 | 改用 Web features/news-center/news-center.js 的 lineIcon('copy') 双矩形原始路径；去掉文字，右侧18px图形、44px点按区。这是已核实的 Web 现有资源，尚未证明与线上 AI Agent 的专用图标完全相同 |
| 麦克风 | Web AI Agent 素材与包内未找到可确认的麦克风，使用 Lucide 补充；Web 包中 Cn 内嵌图标实际为附件，未误作麦克风使用 |
| 新会话 | 默认显示空白引导，保留旧会话/草稿；建议卡片点击只回填，不自动发送 |

## 保留的移动端差异

- 桌面两列弹窗改为单列全屏和历史抽屉；方案选择用底部面板。
- 仅选择方案，不包含客户选择；建议文案去除预设客户姓名。
- 触控按钮加大；输入框适配键盘、安全区，字体用系统字体。
- 宽表格增加局部双向滚动、固定首列和全屏查看。
- 新增按住麦克风、松开转文字、上移取消；声音反馈采用 Web 深灰/青色体系。

## 尚未达到 Web 功能等价的部分

- 真实流式接口、跨端历史同步、附件上传/解析、ASR 未接入。
- Web 完整 Markdown、业务结果/文件卡片尚未移植；已实现过程事件驱动的推理步骤展开、状态与技能标题条件显示，当前服务为本地演示。
- 历史分页与服务端同步尚未接入；本地编辑名称、删除会话已完成。
- iOS、Android、企业微信中的麦克风权限、触控打断及软键盘仍需要真机验证。

## 本轮验证

18 项自动化测试通过：方案与表格数据、流式取消、存储、附件、默认新会话、直接按住/取消/移回、回填、权限竞态与资源释放。
浏览器 390×844 检查：新会话无横向溢出，图片正常加载；建议卡片只回填；回复气泡为 rgb(47,54,64)，复制使用原 Web SVG；长按无额外 modal，原麦克风坐标保持稳定，上移出现取消提示及红色反馈。

## 历史管理补齐

每条历史右侧固定显示更多按钮，复用 Web outlined 的 ellipsis / edit / delete SVG。菜单提供编辑名称与删除会话。名称最多 60 个字，去除首尾空格，拒绝空白；改名保留消息、草稿、方案，显式标题不会被首条发送覆盖。删除需要二次确认；删除当前会话切换到最近其他历史，没有历史则显示空白新对话。删除同时清理内存附件元数据。取消不修改数据。

390×844 浏览器完成新建临时草稿、改名、取消删除、确认删除和当前会话切换检查，临时草稿已清理，原有会话保留。当前为浏览器本地持久化，不调用真实删除/重命名接口。

## 截图反馈调整

- 顶栏只保留 GAIP Agent；无安全区时 52px 高，按钮点击区域 44×44px。语音模式通过初始化参数配置，保留按住时的演示说明，移除顶栏开关。
- 删除首屏底部两行说明。
- 历史抽屉对齐 Web #FBFBFB 底色、白底新建按钮、32px 原始新建 SVG、12px 圆角；选中行 #EEEEEE，内缩12px的3×36px青色标记；日期12px/18px，更多图标18px，点按区域44px。关闭使用原始关闭历史记录.svg。窄屏保留移动端抽屉容器。
- 回答头像20px；删除本地模拟徽标和回答完成标签；复制操作右对齐，16px图形及44px触控高度。
- 复制图标未确认：现有 copy.svg 源自 Web 代码块 CopyOutlined，用户明确指出不同于目标回答区图标；当前本地来源中未定位到目标回答区资源，已请求用户提供对应 Web 页面版本。
- 浏览器390×844核对首屏无横向溢出、无多余说明；历史抽屉原素材正常加载；回答头像20px、复制右对齐。18项功能回归通过。

## 当前版本：过程、反馈与滚动

以上早期截图记录由本节更新：复制已替换为 Web 现有双矩形 SVG，不显示文字。普通回复无技能头；仅收到 skill 事件时显示技能名与16px原始结果图标。推理面板仅在存在 process.steps 时出现，支持进行中、完成、中断、手动展开/收起；流式阶段默认展开，完成后默认收起，手动选择会保留。当前示例显示公开的演示步骤摘要，真实过程接口未接入。

Toast 为屏幕中央80%不透明黑底白字。输入栏顺序：附件、做方案；语音、发送。对话底部白底，无渐变、额外分隔线或阴影；滚动条轨道透明。正文尺寸变化与输入框尺寸变化均在持续跟随模式下滚到底部；主动上滑或展开过程暂停跟随，回到最新消息后恢复。

21项测试通过。390×844浏览器核对：普通对话无技能头、过程一轮；选择方案后明确显示16px技能头与三步过程；完成/回到底部时剩余距离在1px内；Toast中心(195,422)，黑底白字；复制按钮文本为空。过程展开可正常阅读。Web 来源未写入。

## 最新细节调整

- 删除语音成功回填后的可视 Toast，仅保留无障碍状态播报；错误与取消提示仍按原逻辑处理。
- 输入栏做方案按钮按 Web skillToggle 的状态实现：深灰字 #2F3640、10%青色底，选中/悬停/聚焦时保留青到桃色渐变边框，不改成纯青色边框。
- 删除用户消息上方重复的方案名标签，不删除方案请求数据或技能结果标题。
- 复制恢复到正文左侧，纯图标；浏览器检查每条回答的图标容器左边界与正文完全相同。
- 本轮为局部显示调整，执行 JavaScript 语法检查与浏览器样式核对；没有新增测试。Web 来源全程只读。

## 首屏渐变圆球

首屏机器人替换为 Web 入口 V1 VoiceOrb 的动态渐变圆球，保留 52×52px 占位，移除机器人和旧头像外圈。独立模块 src/welcome-orb.js 复用原始着色器；assets/orb-texture.webp 与 orb-poster.png 分别复制自 creative-3.b08cc4fe.webp、creative-3.04cea590.png。没有引入入口切换、机器人视频或 Web 运行时路径。

H5 增加可见性/减少动态效果处理：后台及离屏暂停，开启减少动态效果时使用静态原始渐变图；退出首屏销毁动画和观察器，新对话重新创建。WebGL 不可用时使用同一静态图。

验证：JavaScript 语法检查通过；390×844 首屏无横向溢出，圆球持续变化、机器人已移除；新建对话后只有一个圆球画布，浏览器无错误。来源项目全程只读。

## V1 / V2 头像与首屏横排（2026-09-14）

更新上一节的纯圆球设计：头像按钮点击切换 V1 渐变机器人与 V2 机器人猫，首次默认 V2，本地独立偏好键记住选择。V1 复用原圆球着色器并从现有 agent.svg 提取白色机器人路径；V2 从 Web 只读复制入口版本2.png 和机器人猫客服悬浮入口循环动画v2.mp4。素材在 H5 assets 内，动画不访问麦克风。后台/离屏暂停，减少动态效果使用静态图；版本切换销毁旧动画。

头像52px；右侧标题18px/26px、说明12px/19px，横向间距14px。保留按钮键盘操作、可访问版本说明与轻量 V1/V2 标记。

做方案核对 AI Agent.css、AI Agent改版.css 和 AI Agent本地Mock.js：默认透明底、渐变描边、32px胶囊圆角、14px字、18px原图标；按下及面板展开显示10%青色底。高亮跟随方案面板展开状态，不再跟随已选方案；aria-expanded 同步弹层开关。移动端不保留触摸后的 hover/focus 背景，键盘焦点与桌面悬停仍有反馈，32px可视按钮保留44px纵向点击范围。

验证：21项既有功能测试全部通过。390×844浏览器检查 V1/V2 往返切换、V2视频播放、刷新保留选择、首屏横排；方案打开后背景 rgba(36,212,201,0.098)，关闭后透明，展开属性同步，无浏览器错误。Web 来源无写入。

## 做方案选中态：以用户截图为准

纠正前一节对浅青高亮的理解：方案展开或已选方案时，输入栏“做方案”为 #2F3640 深灰胶囊，图标和文字为白色，右侧独立 × 按钮退出方案选择并清除所选方案。选中态隐藏渐变描边。未选择且面板关闭时恢复默认态。头像及首屏布局本轮不调整。

浏览器已核对截图样式、选择方案后保持深灰态、× 清除选择并恢复默认；JavaScript语法检查通过。仅修改独立H5。
