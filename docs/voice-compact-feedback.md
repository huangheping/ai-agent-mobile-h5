# 语音反馈与已选方案标签

- 正常按住时仅显示「松手后为您转文字」「上移取消」，保留青色声波；去除演示状态行、重复说明、计时显示及内层深色卡片。
- 上移超过取消阈值显示「松手取消」「移回继续说话」，声波与文字转为红色；移回恢复正常状态。权限和错误提示仍保留必要说明。
- 麦克风图片 draggable=false、pointer-events:none；按钮禁用原生拖拽、文字选择、长按菜单和触摸滚动；保留指针捕获及上移取消手势。
- 输入框上方的已选方案标签使用列表同款 agent-result.svg（16px）；文字和关闭图标为主色 #2F3640，底色 #F5F6F8。做方案按钮原有选中胶囊样式保持。
- Chromium 手机模拟通过真实触摸事件验证：长按、上移取消、移回继续、释放回填、取消无回填；无新标签页或脚本错误。320px 无页面横向溢出；标签颜色、图标及关闭功能通过。
- 原有 21 项测试通过，新增原生拖拽/长按菜单阻止与取消手势回归测试通过（语音测试共 7 项）。尚未进行 iPhone 真机验证。
- H5 资源缓存版本 v14；Web 源项目未修改。
