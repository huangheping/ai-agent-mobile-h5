# 长内容 Mock 与手机弹窗

参考 https://gaip.gloryfh.com/web/proposal 的 AI Agent「技能」列表（2026-09-14 只读查看），保留方案类技能名称，扩展为十条本地 Mock 数据。描述依据可见说明整理，用于长标题、长段落、中英文混排和纵向滚动预览，不调用线上技能。未加入客户选择或客户建档功能。原有方案 ID 保留，历史记录仍可关联。

方案图标为16px，与标题首行对齐；标题和说明完整换行，勾选不压缩，选中方案标签也可换行。390×844浏览器确认十条数据、图标16px、没有横向溢出，列表滚动到底剩余距离0.5px。

手机重要提示保留Web声明及素材，在680px以下使用固定可见标题、警示和确认按钮，中间正文独立滚动；高度扣除屏幕上下安全区。320×568小屏检查弹窗边距、按钮完整可见和正文可滚动。Web来源未写入。
